@model ComputerReparatieshop.Web.Models.Employee_Index

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@{ var dummy = Model.Employees.FirstOrDefault();}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => dummy.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => dummy.PayPerHour)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Employees) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PayPerHour)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
